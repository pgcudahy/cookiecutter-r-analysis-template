linters: linters_with_defaults(
    line_length_linter(120),
    indentation_linter(
        indent = 4L,
        hanging_indent_style = c("never"),
        assignment_as_infix = FALSE),
    object_usage_linter = NULL,
    commented_code_linter = NULL)
